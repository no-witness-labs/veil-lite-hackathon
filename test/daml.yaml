sdk-version: 3.5.1
name: veil-test
source: daml
version: 0.1.0
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
data-dependencies:
  - ../.daml/dist/veil-0.1.0.dar
  # CIP-056 interfaces + the OpenZeppelin simple-token implementation used to mint holdings and
  # fund allocations in tests (the concrete registry behind the standard interfaces).
  - ../dars/splice-api-token-metadata-v1-1.0.0.dar
  - ../dars/splice-api-token-holding-v1-1.0.0.dar
  - ../dars/splice-api-token-allocation-v1-1.0.0.dar
  - ../dars/splice-api-token-allocation-instruction-v1-1.0.0.dar
  - ../dars/simple-token-0.1.0.dar
build-options:
  - --target=2.1
